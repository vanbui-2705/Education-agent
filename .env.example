# Chép file này thành ".env" rồi điền key thật vào.
# File ".env" KHÔNG BAO GIỜ đưa lên git (đã chặn trong .gitignore).

# Lấy key miễn phí tại: https://aistudio.google.com/apikey
GEMINI_API_KEY=dan_key_cua_ban_vao_day

# Tên model dùng. Đổi được mà không phải sửa code.
GEMINI_MODEL=gemini-2.5-flash
